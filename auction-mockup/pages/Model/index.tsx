import React from "react";
import Layout from "../components/UI/Layout/layout";
import styles from './modelSpecific.module.scss';

const Model: React.FC = () => {
    return (
        <Layout>
            <div></div>
        </Layout>
    );
};

export default Model; 